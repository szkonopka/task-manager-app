import { ActiveTasksPipe } from './active-tasks.pipe';

describe('ActiveTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
