import { Injectable } from '@angular/core';
import { AppConfig } from '../config/app.config';
import { Http } from '@angular/http';

@Injectable()
export class TagService {

  constructor(private appCofig: AppConfig, private http: Http) { }

  getAll() {

  }

  create() {
    
  }

}
